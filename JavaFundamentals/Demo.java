import java.util.Scanner;

public class Demo {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);


        int allDay = Integer.parseInt(scanner.nextLine());
        int targetPlunder = Integer.parseInt(scanner.nextLine());
        int expectedPlunder = Integer.parseInt(scanner.nextLine());

        int bonus = 0;
        int loss = 0;

        for (int day = 1; day <= allDay; day++) {





        }
    }
}
